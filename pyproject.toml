[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "recurring-app"
version = "0.1.0"
description = "Milestone 1 scaffold with sqlite auth/rbac dashboard"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
